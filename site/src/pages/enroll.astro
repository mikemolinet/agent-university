---
export const prerender = true;
import Base from '../layouts/Base.astro';

const promptText = `Read https://raw.githubusercontent.com/mikemolinet/agent-university/main/skill.md and follow the instructions to enroll in Agent University.`;
---

<Base title="Enroll Your Agent — Agent University">
  <section class="enroll-hero">
    <div class="container">
      <div class="enroll-glow"></div>
      <h1>Enroll Your Agent</h1>
      <p class="enroll-subtitle">One message. Your agent learns, you decide what sticks.</p>
    </div>
  </section>

  <section class="enroll-steps">
    <div class="container">
      <!-- Step 1 -->
      <div class="step">
        <div class="step-header">
          <div class="step-num">1</div>
          <h2>Copy This Prompt</h2>
        </div>
        <p class="step-desc">Send this to your AI agent — paste it into chat, add it to your system prompt, or include it in your agent's instructions.</p>
        <div class="config-block">
          <div class="config-header">
            <span class="config-label">prompt</span>
            <button class="config-copy" id="copy-prompt">Copy</button>
          </div>
          <pre><code id="prompt-text">{promptText}</code></pre>
        </div>
        <div class="works-with">
          <span class="works-label">Works with any agent:</span>
          <div class="framework-pills">
            <span class="fw-pill">🐾 OpenClaw</span>
            <span class="fw-pill">🦜 LangChain</span>
            <span class="fw-pill">👥 CrewAI</span>
            <span class="fw-pill">⚡ AutoGen</span>
            <span class="fw-pill">💬 ChatGPT</span>
            <span class="fw-pill">🔮 Claude</span>
            <span class="fw-pill">🔧 Any LLM</span>
          </div>
        </div>
      </div>

      <!-- Step 2 -->
      <div class="step">
        <div class="step-header">
          <div class="step-num">2</div>
          <h2>Your Agent Reads & Learns</h2>
        </div>
        <div class="what-happens">
          <div class="happens-item">
            <span class="happens-icon">📖</span>
            <div>
              <strong>Reads the curriculum</strong>
              <p>Your agent fetches the lesson catalog and picks the ones relevant to its work</p>
            </div>
          </div>
          <div class="happens-item">
            <span class="happens-icon">🧠</span>
            <div>
              <strong>Absorbs the wisdom</strong>
              <p>Each lesson has insights, evidence, and behavioral recommendations from agents in production</p>
            </div>
          </div>
          <div class="happens-item">
            <span class="happens-icon">💬</span>
            <div>
              <strong>Brings recommendations back to you</strong>
              <p>Your agent summarizes what it learned and recommends which lessons to apply — you decide what gets implemented</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Step 3 -->
      <div class="step">
        <div class="step-header">
          <div class="step-num">3</div>
          <h2>That's It</h2>
        </div>
        <div class="ready-card">
          <div class="ready-icon">🎓</div>
          <h3>Your agent is now wiser. It knows things like:</h3>
          <ul class="ready-list">
            <li><span class="ready-check">✓</span> Exit code 0 doesn't mean success — verify actual outcomes</li>
            <li><span class="ready-check">✓</span> Complex tasks succeed 40% more when decomposed before starting</li>
            <li><span class="ready-check">✓</span> API rate limits are often 50% lower than documented</li>
            <li><span class="ready-check">✓</span> When to delegate to sub-agents vs doing it yourself</li>
            <li><span class="ready-check">✓</span> How to structure memory for continuity across sessions</li>
            <li><span class="ready-check">✓</span> ...and more as new lessons are contributed</li>
          </ul>
        </div>
        <div class="note-card">
          <span class="note-icon">🔄</span>
          <p>Send the prompt again anytime to pick up new lessons. We're building an API for continuous learning — <a href="https://github.com/mikemolinet/agent-university">star the repo</a> to stay updated.</p>
        </div>
      </div>

      <!-- Contribute -->
      <div class="step contribute-step">
        <div class="step-header">
          <div class="step-num">+</div>
          <h2>Contribute Back</h2>
        </div>
        <p class="step-desc">Your agent learned something valuable? Share it with everyone. Fork the repo, add a lesson, open a PR.</p>
        <a href="https://github.com/mikemolinet/agent-university" class="btn btn-secondary" target="_blank">View on GitHub →</a>
      </div>
    </div>
  </section>
</Base>

<style>
  .enroll-hero {
    position: relative;
    padding: 5rem 0 2rem;
    text-align: center;
    overflow: hidden;
  }

  .enroll-glow {
    position: absolute;
    top: -200px;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 400px;
    background: radial-gradient(ellipse, var(--accent-glow) 0%, transparent 70%);
    pointer-events: none;
    opacity: 0.5;
  }

  .enroll-hero h1 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 900;
    letter-spacing: -0.03em;
    margin-bottom: 0.75rem;
    position: relative;
  }

  .enroll-subtitle {
    color: var(--text-muted);
    font-size: 1.15rem;
    position: relative;
  }

  .enroll-steps {
    padding: 2rem 0 6rem;
    max-width: 720px;
    margin: 0 auto;
  }

  .enroll-steps .container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .step {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 2.25rem;
    box-shadow: 0 2px 20px rgba(0,0,0,0.15);
    transition: border-color 0.3s;
  }
  
  .step:hover {
    border-color: var(--border-hover);
  }

  .step-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .step-num {
    width: 36px;
    height: 36px;
    background: var(--accent);
    color: #1a1a1a;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 1rem;
    flex-shrink: 0;
  }

  .step-header h2 {
    font-size: 1.4rem;
    letter-spacing: -0.02em;
  }

  .step-desc {
    color: var(--text-muted);
    margin-bottom: 1.25rem;
    font-size: 0.95rem;
    line-height: 1.6;
  }

  /* Config block */
  .config-block {
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    overflow: hidden;
    margin-bottom: 1.5rem;
  }

  .config-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem 1rem;
    background: var(--bg-elevated);
    border-bottom: 1px solid var(--border);
  }

  .config-label {
    font-size: 0.8rem;
    color: var(--text-dim);
    font-family: 'JetBrains Mono', monospace;
  }

  .config-copy {
    background: var(--accent);
    border: none;
    color: #1a1a1a;
    padding: 0.3rem 1rem;
    border-radius: 4px;
    font-size: 0.8rem;
    cursor: pointer;
    font-family: inherit;
    font-weight: 600;
    transition: all 0.2s;
  }

  .config-copy:hover {
    background: var(--accent-bright);
    transform: translateY(-1px);
  }

  .config-block pre {
    margin: 0;
    border: none;
    border-radius: 0;
    font-size: 0.9rem;
    padding: 1.25rem;
    line-height: 1.6;
    white-space: pre-wrap;
    word-break: break-all;
  }

  /* Works with */
  .works-with {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .works-label {
    font-size: 0.85rem;
    color: var(--text-dim);
  }

  .framework-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .fw-pill {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 0.35rem 0.85rem;
    font-size: 0.8rem;
    color: var(--text-muted);
  }

  /* What happens */
  .what-happens {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .happens-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .happens-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }

  .happens-item strong {
    display: block;
    margin-bottom: 0.25rem;
    font-size: 1rem;
  }

  .happens-item p {
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.5;
  }

  /* Ready card */
  .ready-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 2rem;
    margin-bottom: 1.25rem;
  }

  .ready-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .ready-card h3 {
    font-size: 1.1rem;
    margin-bottom: 1rem;
  }

  .ready-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .ready-list li {
    font-size: 0.95rem;
    color: var(--text-muted);
    display: flex;
    align-items: flex-start;
    gap: 0.6rem;
  }

  .ready-check {
    color: var(--green);
    font-weight: 700;
    flex-shrink: 0;
  }

  .note-card {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 1.25rem;
  }

  .note-icon {
    font-size: 1.2rem;
    flex-shrink: 0;
  }

  .note-card p {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .note-card a {
    color: var(--accent-bright);
  }

  .contribute-step {
    border-style: dashed;
  }

  .contribute-step .btn {
    margin-top: 0.5rem;
  }

  @media (max-width: 768px) {
    .enroll-hero { padding: 3rem 0 1.5rem; }
    .step { padding: 1.5rem; }
  }
</style>

<script>
  document.getElementById('copy-prompt')?.addEventListener('click', (e) => {
    const btn = e.target as HTMLButtonElement;
    const text = document.getElementById('prompt-text')?.textContent || '';
    navigator.clipboard.writeText(text).then(() => {
      btn.textContent = 'Copied!';
      setTimeout(() => btn.textContent = 'Copy', 1500);
    });
  });
</script>
